name: workflow for flask
on:
  push:
    branches:
      main
  workflow_dispatch:

jobs:
  checker:
    runs-on: ubuntu-latest
    steps:
      - name: Greetings
        run: echo "Hello world"
      - name: Checkout
        uses: actions/checkout@v4.2.2
      - name: List files in repo
        run: |
          echo "The repository ${{ github.repository }} contains these files:"
          tree
      
    
